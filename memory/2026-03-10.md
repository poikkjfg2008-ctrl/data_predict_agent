# 2026-03-10

- 用户新需求：将项目各阶段 agent 消息通过 skill 方式组装 context，集成到已部署智能体。
- 交付目标：新增一个可触发的 Skill（含 `SKILL.md`）与配套脚本（`scripts/assemble_stage_context.py`），支持把 `pipeline_results.json` 转为部署可用的 JSON + Markdown 上下文。
- 约束：参考 skill-creator 结构，覆盖全流程阶段（exploration/preprocessing/feature_engineering/model_selection/training/evaluation/prediction）。
